<div class="bsh-tab-body bsh-background-tab {{tabBodySelectionClass "background"}}">
    <h2>{{localize "bsh.headers.base_attributes"}}</h2>
    <p class="bsh-blurb">{{localize "bsh.blurbs.base_attributes"}}</p>
    {{> "systems/black-sword-hack/templates/partials/cs-base-attributes-list.hbs"}}

	<h2>{{localize "bsh.headers.background"}}</h2>
    <div class="bsh-field">
    	<label for="data.birth" class="bsh-label">{{localize "bsh.fields.labels.birth"}}</label>
    	<select name="data.birth" class="bsh-input bsh-select">
		{{#select data.birth}}
		{{#each configuration.birthList as |value|}}
    		<option value="{{value}}">{{localize value}}</option>
		{{/each}}
		{{/select}}
    	</select>
    </div>

	<div class="bsh-field">
    	<label for="data.origin" class="bsh-label">{{localize "bsh.fields.labels.origin"}}</label>
    	<select name="data.origin" class="bsh-input bsh-select">
		{{#select data.origin}}
		{{#each configuration.originList as |label value|}}
    		<option value="{{value}}">{{localize label}}</option>
		{{/each}}
		{{/select}}
    	</select>
    </div>

    <div class="bsh-field">
    {{#backgroundSelect "first" fromOrigin=true}}
		<label for="data.backgrounds.first" class="bsh-label">{{localize labelKey}}</label>
		<select name="data.backgrounds.first" class="bsh-input bsh-select">
		{{#select ../data.backgrounds.first}}
		{{#each options as |label value|}}
			<option value="{{value}}">{{localize label}}</option>
		{{/each}}
		{{/select}}
		</select>
    {{/backgroundSelect}}
    </div>


    <div class="bsh-field">
    {{#backgroundSelect "second" fromOrigin=true}}
		<label for="data.backgrounds.second" class="bsh-label">{{localize labelKey}}</label>
		<select name="data.backgrounds.second" class="bsh-input bsh-select">
		{{#select ../data.backgrounds.second}}
		{{#each options as |label value|}}
			<option value="{{value}}">{{localize label}}</option>
		{{/each}}
		{{/select}}
		</select>
    {{/backgroundSelect}}
    </div>


    <div class="bsh-field">
    {{#backgroundSelect "third" fromOrigin=false}}
		<label for="data.backgrounds.third" class="bsh-label">{{localize labelKey}}</label>
		<select name="data.backgrounds.third" class="bsh-input bsh-select">
		{{#select ../data.backgrounds.third}}
		{{#each options as |label value|}}
			<option value="{{value}}">{{localize label}}</option>
		{{/each}}
		{{/select}}
		</select>
    {{/backgroundSelect}}
    </div>

    <br>
    <h2>{{localize "bsh.headers.random"}}</h2>
    <p class="bsh-blurb">{{localize "bsh.blurbs.random_generation"}}</p>
    <button class="bsh-random-character-generator-button" data-id="{{actor._id}}">{{localize "bsh.buttons.random_generation"}}</button>
</div>
